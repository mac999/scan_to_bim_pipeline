# Scan to BIM pipeline - full dependencies (Python 3.11)
# Install: pip install -r requirements.txt
# If GDAL fails to install with pip, use conda (conda install -c conda-forge gdal pdal)
# or a prebuilt wheel matching your platform.

# core numerical / geometry
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.2
sympy>=1.11
pandas>=2.0
matplotlib>=3.7
Pillow>=10.0

# point cloud processing
open3d>=0.17
laspy>=2.4
trimesh>=3.20
pyvista>=0.42
alphashape>=1.3.1

# GIS / raster / vector
GDAL>=3.6
shapely>=2.0
geopandas>=0.13
pyproj>=3.5
rasterio>=1.3
simplekml>=1.3.6

# imaging
opencv-python>=4.8

# BIM
ifcopenshell>=0.7.0

# utilities
tqdm>=4.65
PyYAML>=6.0

# --- optional: deep learning based segmentation (pcd_to_seg_ml) ---
# Open3D-ML requires a matching torch build. Refer to
# https://github.com/isl-org/Open3D-ML#installation for the tested combination.
# torch>=2.0
# tensorboard>=2.13
